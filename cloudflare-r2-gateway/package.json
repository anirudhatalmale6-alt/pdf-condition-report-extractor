{
  "name": "orbas-download-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Gated download gateway for ORBAS installer (Cloudflare Worker + R2)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
